---
description: Reflect on one completed run result and update family state.
argument-hint: "<path to result.json>"
---

Use `mfm-reflector` and run:

```powershell
python "plugins/model-family-mae/scripts/mfm_cli.py" reflect "$ARGUMENTS"
```

Summarize the reflected run, stage counts, remaining gate requirements, and the next one-knob action.
